---
title: COVID19 in Österreich
layout: base
structure: hmf
version: 1.0.0
created_at: 2021-02-27
updated_at: 2021-02-27
tags: [project]
scripts:
  - /covid19-at/main.js
styles:
  - /covid19-at/main.css
---
{% set defaultProvince = 10 %}
{% set stats = [ 
  { id: 'SiebenTageInzidenzFaelle', name: '7 Tage Inzidenz', higherIsBetter: false },
  { id: 'AnzahlFaelle', name: 'Fälle', higherIsBetter: false },
  { id: 'AnzahlTotTaeglich', name: 'Todesfälle', higherIsBetter: false },
  { id: 'AnzahlGeheiltTaeglich', name: 'Genesene', higherIsBetter: true },
  { id: 'Test', name: 'Tests', higherIsBetter: true }
] %}
<header class="header header--covid19-at">
  <h1><strong>COVID19</strong> in <br>Österreich</h1>
</header>
<main class="main main--covid19-at">
  <div class="covid19-table">
    <div class="covid19-table__select">
      <select id="select-7ti">
        {% for province in provinces %}
          <option value="{{ province.BundeslandID }}" {{ "selected" if province.BundeslandID === defaultProvince }}>
            {{ province.Bundesland }}
          </option>
        {% endfor %}
      </select>
    </div>
    <div class="covid19-table__header">Gestern<br><span class="date">{{ dates.yesterday }}</span></div>
    <div class="covid19-table__header">Vorgestern<br><span class="date">{{ dates.beforeYesterday }}</span></div>
    <div class="covid19-table__header">Vorige Woche<br><span class="date">{{ dates.lastWeek }}</span></div>
    <div class="covid19-table__header">Voriges Monat<br><span class="date">{{ dates.lastMonth }}</span></div>
    {% for province in provinces %}
      {% for stat in stats %}
        {% set row = timeline | covid19AtRowByProvince(province.BundeslandID) %}
          <div class="covid19-table__cell covid19-table__stat-name {{ "hide" if province.BundeslandID !== defaultProvince }}" data-bundesland="{{ province.BundeslandID }}" data-stat="{{ stat.id }}">{{ stat.name }}</div>
          <div data-bundesland="{{ province.BundeslandID }}" data-stat="{{ stat.id }}" class="covid19-table__cell covid19-table__value {{ "hide" if province.BundeslandID !== defaultProvince }}">
            {{ row.yesterday[stat.id] | round | formatNumber }}
          </div>
          <div data-bundesland="{{ province.BundeslandID }}" data-stat="{{ stat.id }}" class="covid19-table__cell covid19-table__value {{ "hide" if province.BundeslandID !== defaultProvince }}">
            {{ row.beforeYesterday[stat.id] | round | formatNumber }}
            {% covid19AtDiffBetweenValues row.yesterday[stat.id], row.beforeYesterday[stat.id], stat.higherIsBetter %}
          </div>
          <div data-bundesland="{{ province.BundeslandID }}" data-stat="{{ stat.id }}" class="covid19-table__cell covid19-table__value {{ "hide" if province.BundeslandID !== defaultProvince }}">
            {{ row.lastWeek[stat.id] | round | formatNumber }}
            {% covid19AtDiffBetweenValues row.yesterday[stat.id], row.lastWeek[stat.id], stat.higherIsBetter %}
          </div>
          <div data-bundesland="{{ province.BundeslandID }}" data-stat="{{ stat.id }}" class="covid19-table__cell covid19-table__value {{ "hide" if province.BundeslandID !== defaultProvince }}">
            {{ row.lastMonth[stat.id] | round | formatNumber }}
            {% covid19AtDiffBetweenValues row.yesterday[stat.id], row.lastMonth[stat.id], stat.higherIsBetter %}
          </div>
        {% endfor %}
      {% endfor %}
  </div>
  <p>Datenquellen: BMSGPK, <a href="https://www.data.gv.at/covid-19">Österreichisches COVID-19 Open Data Informationsportal</a>
</main>