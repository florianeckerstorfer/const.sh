---
title: COVID19 Data Austria
layout: base
structure: hmf
version: 1.0.0
created_at: 2020-10-24T15:30:07.645Z
updated_at: 2020-10-24T15:30:07.645Z
scripts:
  - /covid19-at/main.js
styles:
  - /covid19-at/main.css
---
{% set defaultProvince = "10" %}
{% set stats = [ 
  { id: 'SiebenTageInzidenzFaelle', name: '7 Tage Inzidenz' },
  { id: 'AnzahlFaelle', name: 'Fälle' },
  { id: 'AnzahlTotTaeglich', name: 'Todesfälle' },
  { id: 'AnzahlGeheiltTaeglich', name: 'Genesene' }
] %}
<header class="header header--covid19-at main">
  <h1>COVID19 in <br>Österreich</h1>
</header>
<main class="main">
  <section>
    <table class="overview-table">
      <thead>
        <tr>
          <th>
            <select id="select-7ti">
              {% for province in provinces %}
                <option value="{{ province.BundeslandID }}" {{ "selected" if province.BundeslandID === defaultProvince }}>
                  {{ province.Bundesland }}
                </option>
              {% endfor %}
            </select>
          </th>
          <th>Gestern</th>
          <th>Vorgestern</th>
          <th>Vorige Woche</th>
          <th>Voriges Monat</th>
      </thead>
      <tbody>
        {% for province in provinces %}
          {% for stat in stats %}
          {% set row = timeline | covid19AtRowByProvince(province.BundeslandID) %}
          <tr  class="data-row {{ "hide" if province.BundeslandID !== defaultProvince }}">
            <td class="row-header">{{ stat.name }}</td>
            <td>{{ row.yesterday[stat.id] | round }}</td>
            <td>{{ row.beforeYesterday[stat.id] | round }}</td>
            <td>{{ row.lastWeek[stat.id] | round }}</td>
            <td>{{ row.lastMonth[stat.id] | round }}</td>
          </tr>
          {% endfor %}
        {% endfor %}
      </tbody>
    </table>
  </section>
  <section>
    <h2 id="sieben-tage-inzidenz">
      <a href="#sieben-tage-inzidenz">7 Tage Inzidenz</a>
    </h2>
    <div id="chart-7ti"></div>
  </section>
</main>